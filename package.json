{
  "name": "lab2-testing-project",
  "version": "1.0.0",
  "description": "video game testing project",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "test:unit": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.5",
    "cypress": "^4.2.0",
    "eslint": "^10.2.1",
    "vite": "^8.0.10",
    "vitest": "^4.1.5"
  }
}
